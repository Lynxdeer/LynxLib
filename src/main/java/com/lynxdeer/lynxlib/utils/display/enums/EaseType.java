package com.lynxdeer.lynxlib.utils.display.enums;

public enum EaseType {
	LINEAR, // No easing
	IN, // easeInQuint on easings.net, starts small and gets large towards the end
	OUT, // easeOutQuint on easings.net, starts large and gets small towards the end
	IN_OUT // easeInoutQuint on easings.net
}
